# Auditoria GA4 — 2026-06
> HIST-7.1 do pacote `NotaFacil-Specs-v1`.

## 1. Property ID

- **Variável**: `NEXT_PUBLIC_GA_MEASUREMENT_ID`
- **Status**: a configurar no Vercel (envprod). Sem ela, GA4 fica desativado silenciosamente.

## 2. Páginas instrumentadas

Todas — o script é carregado no `app/layout.tsx` root, condicional à env var.

## 3. Eventos disparados hoje

Por `lib/analytics/events.ts`:

| Evento | Disparado em |
|---|---|
| `cta_click` | Todo CTA do Hero, PricingCard, ComparativoPage, GatewayPage, CookieBanner |
| `pricing_view` | `PricingSection` (IO ≥ 1s) |
| `comparison_view` | `CompetitorTable` (IO ≥ 0.2s) |
| `topbar_view`/`topbar_dismiss` | `UrgencyTopBar` |
| `sandbox_open` | _(a instrumentar em `/sandbox`)_ |
| `signup_start`/`signup_complete` | _(a instrumentar nos handlers de cadastro)_ |
| `first_nfse_created`/`first_nfse_authorized` | _(a instrumentar — server-side, via API Go)_ |

## 4. Custom dimensions a criar no painel GA4

| Nome | Escopo |
|---|---|
| `persona` | event |
| `cta_location` | event |
| `plan` | event |
| `experiment_id` | event |
| `variant` | event |
| `page_section` | event |
| `utm_first_source` | user |
| `utm_first_medium` | user |
| `utm_first_campaign` | user |

## 5. Consent Mode v2

✅ Implementado:
- `lib/analytics/gtag.ts` — script `gtag('consent','default',{denied})` antes do GA4 carregar
- `components/consent/CookieBanner.tsx` — botões Aceitar/Rejeitar persistem em cookie `nf_consent` (12 meses)
- Sem aceite, GA4 não envia eventos

## Gaps

- [ ] Criar property GA4 e adicionar `NEXT_PUBLIC_GA_MEASUREMENT_ID` no Vercel
- [ ] Criar as 9 custom dimensions no painel
- [ ] Validar 1 evento por persona em DebugView
- [ ] Instrumentar `sandbox_open`, `signup_*`, `first_nfse_*`
