[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"
buildArgs = { SERVICE_PATH = "apps/api" }

[deploy]
startCommand = "./server"
healthcheckPath = "/v1/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
